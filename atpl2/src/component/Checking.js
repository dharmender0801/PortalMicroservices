import React from "react";
import NavMenu from "./NavMenu";

const Checking = props => {
    return (
        <div>
            <h1>{props.name.servicename}</h1>
        </div>
    );
}
export default Checking;